# Pasta `docs/` — site publicado no GitHub Pages

Conteúdo gerado pelo build (`npm run build:pages` ou workflow **Deploy GitHub Pages**).

**Não edite à mão.** O `index.html` da raiz do repositório é só para desenvolvimento.

No GitHub: **Settings → Pages → Branch `main` → Folder `/docs`**
